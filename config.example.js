// Copy this file to config.js and replace the placeholder with your key.
// Never commit config.js. It is ignored by .gitignore.
window.WEATHER_CONFIG = {
  OPENWEATHER_API_KEY: "YOUR_OPENWEATHER_API_KEY",
};
